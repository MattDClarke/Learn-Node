mixin reviewForm(store)

    form.formReview(action=`/reviews/${store._id}` method="POST") 
        div.form-group
            textarea(name="text" placeholder="Did you try this place? Have something to say? Leave a review...")
            small Error message

            //- Character count
            span Min 3 characters, Max: 1000 characters 
            span.textAreaCharCountContainer
                span.textAreaCharCount 
                span.textAreaCharCountMax  / 1000
            
        .reviewer__meta
            .reviewer__stars      
                each num in [5,4,3,2,1]   
                    //- input hidden using css. stars styled with label
                    input(type="radio" id=`star${num}` name="rating" value=num)
                    label(for=`star${num}`) #{num} Stars
            input.button(type="submit" value="Submit Review ➡")

        