mixin review(review)
    .review__header 
        .review__author
            img.avatar(src=review.author.gravator)
            p= review.author.name
        //- title for accessibility - mostly shows as a tootip on mouse hover
        .review__stars(title=`Rated ${review.rating} out of 5 stars`)
            = `★`.repeat(review.rating)
            = `☆`.repeat(5 - review.rating)
        time.review__time(datetime=review.created)= h.moment(review.created).fromNow()
    .review__body 
        p= review.text
